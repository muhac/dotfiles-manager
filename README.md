# Dotfiles Manager

My dotfiles and personal preferences.

Managed using [GNU Stow](https://www.gnu.org/software/stow/).

## Install

```bash
curl -fsSL https://muhac.github.io/dotfiles-manager/install.sh | bash
```

Clones the repo to `~/.dotfiles` and creates all symlinks automatically.
