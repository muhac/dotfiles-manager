# Global Conventions

- Commit format: `feat/fix/chore/refactor(scope): message`
- Comments only when the _why_ is non-obvious
- For non-trivial changes, write tests first (TDD). Prefer integration tests over mocks.
